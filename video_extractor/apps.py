"""Configuration for the video_extractor app."""

from django.apps import AppConfig


class VideoExtractorConfig(AppConfig):
    """Configuration for the video_extractor app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "video_extractor"
